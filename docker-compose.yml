version: "3.8"
services:
  stock:
    container_name: stockphoto
    image: stockphotodev
    build:
      context: .
      dockerfile: Dockerfile.dev
    volumes:
      - '.:/app'
      
    ports:
      - 3002:3000
    environment:
      - CHOKIDAR_USEPOLLING=true

      
